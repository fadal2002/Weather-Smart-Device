#include <iostream>
#include <fstream>
#include <string>

using namespace std;

class EncryptDecrypt
{
public:
	void logEncryptor(string fileName);
	void logDecryptor(string fileName);
	void writeEncrypt(string fileName);
	void writeDecrypt(string fileName);
};